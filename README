BARE-METAL

Everything is built from scratch without using any libraries 
and IDEs for embedded development. The project should 
be done with an Unix, make and gnu-arm-embedded-toolchain.

Project uses STM32F405RG MCU.
